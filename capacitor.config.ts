import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'get-token-admin',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
